<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home - DocBook</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <!-- Link to external stylesheet -->
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <!-- Include the navbar (with patient conditional) -->
  <%- include("../includes/navbar.ejs", { patient: patient }) %>
  <div class="home-container">
    <div class="home-header">
      <% if (loggedIn && patient && patient.name) { %>
      <div class="hero-content-hero">
        <h1>Welcome <%= patient.name %> to our clinic.</h1>
      </div>
      <% } else { %>
        <div class="hero-content-hero">
          <h1>Welcome to our clinic.</h1>
          <span class="button-container">
            <a href="/register" class="btn-primary">Register</a>
            <a href="/login" class="btn-secondary">Login</a>
          </span>
        </div>
      <% } %>
    </div>
  </div>
  <%- include("../includes/homeContent.ejs") %>
  <%- include("../includes/footer.ejs") %>
</body>
</html>
